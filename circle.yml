machine:
  node:
    version: 0.12.0
general:
  branches:
    only:
      - master
test:
  override:
    - echo 'no test!'
deployment:
  production:
    branch: master
    commands:
      - npm run deploy

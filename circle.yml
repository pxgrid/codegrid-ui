machine:
  node:
    version: 4.1.0
general:
  branches:
    only:
      - master
test:
  override:
    - echo 'no test!'
deployment:
  production:
    branch: master
    commands:
      - npm run deploy

machine:
  node:
    version: 0.12.0
general:
  branches:
    only:
      - master
dependencies:
  post:
    - npm rebuild node-sass
deployment:
  production:
    branch: master
    commands:
      - npm run deploy
